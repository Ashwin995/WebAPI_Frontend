function ListJobPerEmployer(props) {
    let job = props.job;
    const list = job.map((job) => {
        <td>{list}</td>
    });
    return (
        <tr>{list}</tr>
    )
}

export default ListJobPerEmployer;